# Core data and audio processing
numpy>=2.0,<3
pandas>=2.2,<3
scipy>=1.13,<2
librosa>=0.10.2,<0.11
soundfile>=0.12.1,<1
python-dotenv>=1.0,<2

# Classical ML baseline and evaluation
scikit-learn>=1.5,<2
xgboost>=2.1,<3
joblib>=1.4,<2

# Deep learning: install the correct PyTorch build for your machine.
# The CPU-safe package is listed here; replace it with the CUDA command
# from https://pytorch.org/get-started/locally/ when training on an NVIDIA GPU.
torch>=2.4,<3
torchaudio>=2.4,<3

# Experiment and dataset utilities
matplotlib>=3.9,<4
seaborn>=0.13,<1
pyyaml>=6.0,<7
tqdm>=4.66,<5

# Training/inference API support
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9,<1

# Tests
pytest>=8.3,<9
